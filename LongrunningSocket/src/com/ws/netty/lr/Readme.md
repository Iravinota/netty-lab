一个完整的长连接客户端和服务端，实现了如下功能：

1. 心跳检测：IdleStateHandler。客户端可以只发送不接收数据，长时间接收不到消息时，也会自动进行心跳检测。若接收不到心跳返回包，则断掉重连。
2. 交互报文为BaseMsg
3. 断掉重连：doConnect
4. 自定义发送数据: CustDevInfo
5. 交互式发送数据
6. 客户端使用多个channel连接服务端，随机选择一个channel进行发送